# Avance 2026-05-10

## Contexto

Objetivo de la jornada: avanzar la igualacion del sistema local contra `https://kamary.l105.com/inicio/ingresar`, con foco en Magistrales, estabilidad de guardados y reportes paginados con exportacion PDF.

Repositorio local: `c:\xampp\htdocs\kamary.pe`

## Hecho hoy

- Se restauro desde git la configuracion de pruebas que se habia eliminado:
  - `phpunit.xml`
  - `.env.testing`
- Se creo la base local `kamary_testing` porque PHPUnit fallaba con `Unknown database 'kamary_testing'`.
- Se mantuvo la limpieza previa de logs temporales `.tmp_docker_up*.log`.

## Magistrales

- Se continuo dejando operativo el bloque Magistrales con modulos y submodulos agregados/ajustados:
  - Articulos Magistrales
  - Laboratorios Magistrales
  - Proveedores Magistrales
  - Unidades Magistrales
  - Categorias
  - Formatos
  - Responsables
  - Formulas
  - Ingresos
  - Inventario
  - Kardex
  - Ordenes de produccion
  - Salidas
  - Ventas
  - Ordenes de compra Magistrales
- Se agrego blindaje transaccional para guardados con cabecera + detalle, evitando registros parciales si falla algun item.
  - Nuevo trait: `app/Http/Controllers/Admin/Magistrales/Concerns/RunsMagistralSaveInTransaction.php`
  - Aplicado en formulas, ingresos, inventario, salidas, ordenes de produccion y ventas.
  - Ordenes de compra Magistrales tambien quedo envuelto en transaccion.

## Reportes y PDF

- Antes, las grillas exportables solo generaban Excel.
- Se agrego exportacion PDF global para grillas DevExtreme exportables:
  - `resources/js/Utils/dataGridExport.js`
  - `resources/js/Components/Adminto/DataGrid.jsx`
  - `resources/js/Components/Tailwind/DataGrid.jsx`
- Se cargaron dependencias locales ya existentes:
  - `exceljs.min.js`
  - `FileSaver.min.js`
  - `jspdf.umd.min.js`
  - `jspdf.plugin.autotable.min.js`
  - Archivo: `resources/views/admin.blade.php`
- Se corrigio el conteo de reportes agrupados/subqueries en:
  - `app/Http/Controllers/Admin/ReportController.php`
- Bug encontrado y corregido:
  - El reporte de inventario mostraba muchas filas, pero el paginador decia `Pagina 1 de 1 (1 items)`.
  - Despues del parche muestra correctamente `Pagina 1 de 2 (30 items)` en local.

## Validaciones realizadas

- `php -l` en controladores PHP tocados: OK.
- `php artisan route:list --path=admin/magistrales`: OK.
- `npm run build`: OK.
- `php artisan test`: OK.
  - 12 tests pasaron.
  - 40 assertions.
- Validacion en navegador con Playwright:
  - Login local con usuario admin temporal.
  - Acceso a `/admin/reports/inventory`.
  - Paginado verificado.
  - Exportacion PDF verificada.
  - PDF generado: `reporte-de-inventario.pdf`, aprox. 100 KB.
- Importante: para probar UI se cambiaron temporalmente hashes de usuarios locales y luego se restauraron.

## Archivos clave tocados hoy

- `app/Http/Controllers/Admin/ReportController.php`
- `app/Http/Controllers/Admin/Magistrales/Concerns/RunsMagistralSaveInTransaction.php`
- `app/Http/Controllers/Admin/Magistrales/FormulaController.php`
- `app/Http/Controllers/Admin/Magistrales/IncomeController.php`
- `app/Http/Controllers/Admin/Magistrales/InventoryController.php`
- `app/Http/Controllers/Admin/Magistrales/OutputController.php`
- `app/Http/Controllers/Admin/Magistrales/ProductionOrderController.php`
- `app/Http/Controllers/Admin/Magistrales/PurchaseOrderController.php`
- `app/Http/Controllers/Admin/Magistrales/SaleController.php`
- `resources/js/Utils/dataGridExport.js`
- `resources/js/Components/Adminto/DataGrid.jsx`
- `resources/js/Components/Tailwind/DataGrid.jsx`
- `resources/views/admin.blade.php`
- `public/build/manifest.json`

## Pendiente para continuar manana

1. Recomparar Magistrales contra la web de referencia, modulo por modulo, ya con lo nuevo en local:
   - Orden del menu.
   - Nombres visibles.
   - Campos por formulario.
   - Columnas de tablas.
   - Estados y acciones.
2. Probar CRUD completo en Magistrales:
   - Crear/editar/desactivar/eliminar cuando aplique.
   - Validaciones de campos obligatorios.
   - Validaciones de cantidades y stock.
   - Edicion de items en modales.
3. Probar flujos operativos completos:
   - Ingreso suma stock.
   - Salida descuenta stock.
   - Venta descuenta stock si no es cotizacion.
   - Orden de produccion registra producto terminado correctamente.
   - Inventario muestra diferencia real vs sistema.
   - Kardex refleja movimientos en orden correcto.
4. Validar reportes PDF restantes:
   - Reporte de ventas.
   - Resumen diario.
   - Cualquier otra tabla con `exportable`.
5. Revisar permisos y roles:
   - Confirmar que cada modulo Magistrales usa el permiso correcto.
   - Confirmar que usuarios no admin ven solo lo que corresponde.
6. Revisar deploy:
   - Confirmar migraciones nuevas.
   - Confirmar assets build actualizados.
   - Confirmar que no se suban archivos temporales ni `.phpunit.result.cache`.
   - Confirmar que `public/build` quede coherente con la estrategia del proyecto.

## Comandos utiles para retomar

```powershell
php artisan route:list --path=admin/magistrales
php artisan test
npm run build
git status --short
```

## Nota para el siguiente chat

Leer primero este archivo y tambien:

- `docs/checklist-modulos-referencia-kamary-l105.md`
- `routes/api.php`
- `routes/web.php`
- `resources/js/Components/Adminto/Menu.jsx`

Prioridad sugerida: terminar QA funcional de Magistrales antes de seguir agregando modulos nuevos.

## Continuacion 2026-05-10

- Se retomo la comparacion controlada de Magistrales contra `kamary.l105.com`, sin descarga masiva de datos: solo encabezados, labels y botones visibles.
- Articulos:
  - Referencia revisada: `/produccion/almacen/articulo`.
  - Local ajustado: Acciones al inicio, accion Mostrar/Editar, Estado visible en modal y Unidades por caja.
- Categoria:
  - Referencia revisada: `/produccion/almacen/categoria`.
  - Local ajustado: Acciones al inicio, Estado visible en modal y subcategorias.
  - Nuevo soporte: `magistral_subcategories`, modelo `MagistralSubcategory`, endpoints `/api/admin/magistrales/categories/{categoryId}/subcategories` y UI con conteo de articulos por subcategoria.
- Formatos:
  - Referencia revisada: `/produccion/almacen/formatos`.
  - Local ajustado: Acciones al inicio y Estado visible en modal.
- Formulas:
  - Referencia revisada: `/produccion/almacen/recetaproduccion`.
  - Local ajustado con Acciones/Historial, tabla de insumos, campos SOP y motivo de actualizacion.
  - Nuevo soporte backend: `magistral_formula_items`, campos estructurados en `magistral_formulas`, snapshot en `magistral_formula_histories`.
  - Migracion aplicada: `2026_05_10_000002_expand_magistral_formulas_structured_detail.php`.
  - `npm run build`: OK despues del cambio.

## Continuacion parte 2 - stock y Kardex Magistrales

- Se corrigio el endpoint de stock por almacen de Articulos Magistrales para usar el calculo propio de `MagistralesStock`, no el stock de Almacen normal.
- Salidas Magistrales ahora bloquea cantidades mayores al stock disponible y contempla la cantidad del documento actual al editar.
- Ventas Magistrales ahora bloquea stock insuficiente solo cuando no es cotizacion; las cotizaciones no descuentan stock.
- Kardex Magistrales ahora tiene detalle de transacciones por articulo/almacen:
  - Ingreso.
  - Salida.
  - Venta.
  - Produccion finalizada.
  - Consumo de insumos por produccion finalizada.
  - Saldo acumulado en orden cronologico.
- O. Produccion finalizada ahora suma el producto terminado y descuenta los insumos del detalle en el stock base.
- O. Produccion bloquea finalizacion si los insumos superan el stock disponible.
- Nuevo endpoint: `/api/admin/magistrales/kardex/movements`.
- Nueva prueba: `tests/Feature/MagistralesStockFlowsTest.php`.
- Validacion puntual: `php artisan test --filter=MagistralesStockFlowsTest` OK.
- `npm run build`: OK despues del cambio de Kardex.

## Continuacion parte 2 - lote y vencimiento Magistrales

- `MagistralesStock` ahora acepta filtros opcionales de lote y fecha de vencimiento sin romper el calculo global por articulo/almacen.
- Salidas Magistrales valida doblemente:
  - stock total disponible por articulo/almacen;
  - stock disponible del lote/vencimiento cuando el item lo especifica.
- Inventario Magistrales ya no confia en el `system_stock` enviado por el navegador: lo recalcula en servidor por articulo, almacen, lote y vencimiento antes de guardar la diferencia.
- La pantalla de Inventario Magistrales ahora consulta el stock calculado desde `/api/admin/magistrales/inventory/stock` y muestra `Stock sistema` como campo calculado.
- Se agrego cobertura para ingresos por lotes distintos, bloqueo de salida por lote insuficiente y calculo de diferencia de inventario con stock servidor.
- Validacion puntual: `php artisan test --filter=MagistralesStockFlowsTest` OK.

## Pre-produccion - permisos y tests de modulos

- Se agrego sincronizador central de permisos:
  - `app/Support/ModulePermissions.php`
- Se agrego seeder idempotente:
  - `database/seeders/ModulePermissionsSeeder.php`
  - `DatabaseSeeder` ahora lo ejecuta despues de `RoleSeeder`.
- Se agrego migracion de respaldo para produccion:
  - `2026_05_10_000003_sync_module_permissions_for_production.php`
  - Crea/actualiza permisos actuales, crea roles `Admin`/`Root` si faltan y asigna todos los permisos a esos roles.
- Se agrego test de readiness:
  - `tests/Feature/MagistralesModuleReadinessTest.php`
  - Valida permisos sembrados, asignacion a Admin, orden del menu Magistrales, render de las 15 pantallas y endpoints `paginate`.
- Validaciones finales:
  - `php artisan migrate`: OK, nueva migracion aplicada.
  - `php artisan db:seed --class=ModulePermissionsSeeder`: OK e idempotente.
  - `php artisan test`: OK, 19 tests, 261 assertions.
  - `npm run build`: OK.

## Pre-produccion - datos iniciales Magistrales

- Se agrego seeder idempotente para poblar Magistrales en ambientes sin data:
  - `database/seeders/MagistralesProductionSeeder.php`
- El seeder crea datos genericos de arranque, sin credenciales ni datos de negocio reales:
  - 10 unidades, laboratorios, principios activos, categorias, subcategorias, formatos, proveedores, responsables, articulos, formulas, ordenes de compra, ingresos, ordenes de produccion, salidas, ventas e inventarios.
  - Datos base de empresa/sede/almacen para operar Magistrales, con facturacion fiscal marcada como pendiente de configurar.
- Se conecto al arranque Docker:
  - `docker-entrypoint.sh` ejecuta migraciones, `ModulePermissionsSeeder` y `MagistralesProductionSeeder` con reintentos.
  - Al subir el contenedor, despues de migrar, se pobla automaticamente Magistrales si aun no existe la data de arranque.
- `DatabaseSeeder` tambien llama a `MagistralesProductionSeeder`, por si se usa el flujo manual `php artisan db:seed`.
- Se agrego prueba:
  - `tests/Feature/MagistralesProductionSeederTest.php`
  - Valida poblado por modulo, detalle operativo minimo, stock/kardex y que correr el seeder dos veces no duplique registros.
- Validaciones puntuales:
  - `php -l database/seeders/MagistralesProductionSeeder.php`: OK.
  - `php artisan db:seed --class=MagistralesProductionSeeder`: OK e idempotente.
  - `bash -n docker-entrypoint.sh`: OK.
  - `php artisan test --filter=MagistralesProductionSeederTest`: OK, 2 tests, 24 assertions.
  - `php artisan db:seed --class=DatabaseSeeder --env=testing`: OK.
  - `php artisan test`: OK, 21 tests, 285 assertions.
  - Conteo local de muestra despues de seed: 10 articulos, 10 proveedores ficticios, 10 ingresos, 10 ventas y 10 inventarios Magistrales con prefijos de arranque.

## Continuacion - PDF por registro Magistrales

- Se reviso la referencia de forma controlada, extrayendo solo encabezados y acciones visibles.
- Modulos con PDF por fila confirmados en referencia:
  - Ingresos: accion `Imprimir Entrada`.
  - Inventario: accion `Imprimir Ajuste de Inventario`.
  - O. Compra: accion `Imprimir Orden de Compra`.
  - O. Produccion: accion `Imprimir Orden de Produccion`.
  - Salidas: accion `Imprimir Salida`.
- Local actualizado:
  - Nuevo utilitario `resources/js/Utils/magistralesRecordPdf.js`.
  - Boton PDF por fila agregado en Ingresos, Inventario, O. Compra, O. Produccion y Salidas.
  - Boton PDF agregado tambien en Ventas por ser documento operativo; la referencia no mostro filas de ventas para confirmar accion por registro.
- La generacion usa `jsPDF` y `AutoTable`, ya cargados en `resources/views/admin.blade.php`; abre el PDF en una nueva pestana y permite imprimir o guardar como PDF.
- Validacion:
  - `npm run build`: OK.

## Continuacion - PDF por registro fuera de Magistrales

- Se revalido la referencia de forma controlada, por rutas candidatas y sin guardar credenciales ni datos de negocio.
- Modulos adicionales con PDF por fila confirmados:
  - Administracion / Cuentas por pagar: accion PDF por cuenta, ruta visible `/almacen/reportealmacen/{id}`.
  - Muestras / Pedido: accion PDF de guia, ruta visible `/Almacen/imprimirguiaremision_pdf/{id}`.
  - Serv. Almacenamiento / Nota de entrada: acciones PDF de documento y acta, rutas visibles `/almacenamiento/imprimirdocumento/{id}` y `/almacenamiento/actaNotaEntrada/{id}`.
  - Serv. Almacenamiento / Nota de salida: accion PDF de documento, ruta visible `/almacenamiento/imprimirdocumento/{id}`.
  - Serv. Almacenamiento / Facturacion: accion de comprobante/pre-factura; local ya tenia boton PDF por registro en `BillingDocuments`.
- Modulos revisados sin PDF por fila visible en las filas actuales:
  - Almacen / Nota de entrada.
  - Almacen / Nota de salida.
  - Administracion / Gasto.
  - Comercial / Cuenta por cobrar.
  - Comercial / Pedido.
  - Servicios / Orden de servicio.
  - Serv. Almacenamiento / O. Servicio y O. Servicio General.
- Local actualizado:
  - `AccountsPayable.jsx`: boton `Imprimir PDF` por fila.
  - `SampleOrders.jsx`: boton `Imprimir PDF` por fila.
  - `EntryNotes.jsx`: boton `Imprimir PDF` por fila solo en contexto Serv. Almacenamiento.
  - `ExitNotes.jsx`: boton `Imprimir PDF` por fila solo en contexto Serv. Almacenamiento.
  - `magistralesRecordPdf.js`: se ampliaron constructores para cuentas por pagar, notas de almacenamiento y pedido de muestra.
